Put the Brown Sugar font file in this folder, named exactly:

  BrownSugar.woff2   (preferred - smaller file)
  or
  BrownSugar.ttf

The site is already set up to load it automatically (see src/index.css).
Until the file is here, the TWC logo falls back to the Italiana font.
